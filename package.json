{
  "name": "miriam-art",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Portfolio website for a painter. Eleventy + WebC, open web standards first.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "npm run typecheck && eleventy --config=eleventy.config.ts",
    "serve": "eleventy --config=eleventy.config.ts --serve",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-img": "^7.0.0",
    "@11ty/eleventy-plugin-webc": "^0.11.2",
    "@types/node": "^26.2.0",
    "potrace": "^2.1.8",
    "typescript": "^7.0.2"
  }
}
